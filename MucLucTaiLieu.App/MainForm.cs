namespace MucLucTaiLieu.App;

/// <summary>
/// Application shell placeholder. The 3-step wizard (Steps 1-3) and the WebView2
/// A4 host are built in later phases (P3-P6); this stub only establishes the
/// window so the solution compiles and runs on Windows.
/// </summary>
public partial class MainForm : Form
{
    public MainForm()
    {
        Text = "Tạo Mục Lục Hồ Sơ";
        Width = 1200;
        Height = 800;
        StartPosition = FormStartPosition.CenterScreen;
    }
}
